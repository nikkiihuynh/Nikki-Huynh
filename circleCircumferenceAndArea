# Nikki Huynh
# 7/1/22
# Description: Program that computes the circumference and area (rounded to the nearest tenth) of a circle after validating given input radius from user.

# define variable PI
PI = 3.1415
# ask the user to enter radius
radius = float(input('enter radius:'))

# validate input
if radius < 0:
  print('error, radius cannot be negative')
else:
  #calculate area
  area = PI*radius*radius
  #calculate circumference
  circumference = 2*PI*radius

  #show results
  print('a circle with radius %.1f inches has' %radius)
  print('area: %.1f square inches' %area)
  print('circumference: %.1f inches' %circumference)

'''
>>>
enter radius:12
a circle with radius 12.0 inches has
area: 452.4 square inches
circumference: 75.4 inches

#sample run with negative radius
enter radius:-2
error, radius cannot be negative
>>>
'''
